export type ProcesosFiltersAuxiliar = {
  idProceso?: number | string;
  fechaInicio?: string;
  fechaFin?: string;
  resultado?: string;
};

export type ProcesosForFilter = {
  id: number;
  nombre: string;
};